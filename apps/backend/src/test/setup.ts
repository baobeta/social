import { beforeAll, afterAll } from 'vitest';

beforeAll(async () => {
  // Setup test database, Redis, etc.
  // Example: Start testcontainers
});

afterAll(async () => {
  // Cleanup test resources
});
