# CI/CD Environment Variables

# This is for demo purpose :)) -> If in real product, we will follow belows:

# For sensitive values (like JWT_SECRET), use Secrets instead:
# Repository Settings > Secrets and variables > Actions > Secrets

# Node.js Configuration
NODE_VERSION=20

# PostgreSQL Configuration
POSTGRES_VERSION=15
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=social_media_test
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Redis Configuration
REDIS_VERSION=7-alpine
REDIS_URL=redis://localhost:6379
REDIS_PORT=6379

# Database URLs (constructed from above)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/social_media_test
TEST_DATABASE_URL=postgresql://postgres:postgres@localhost:5432/social_media_test
E2E_DATABASE_URL=postgresql://postgres:postgres@localhost:5432/social_media_e2e

# Application Configuration
# Note: JWT_SECRET should be set as a GitHub Secret, not a variable
JWT_SECRET=test-jwt-secret-key-for-ci
NODE_ENV=test
